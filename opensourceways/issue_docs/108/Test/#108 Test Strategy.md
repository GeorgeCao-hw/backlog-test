# #108 数据中台社区会议信息MCP Server测试策略设计说明书

## 1. 基本信息

* **需求链接**: https://github.com/opensourceways/backlog/issues/108
* **需求名称**: 数据中台社区会议信息MCP Server开发
* **核心目标**:
  验证4个MCP Server的功能正确性，以及端到端数据查询流程的集成测试。
* **开发责任人**: **Kaede10** _a,b,c_
* **测试责任人**: **guoxiaozhen0304** _a,b,c_

---

## 2. 测试维度确认

> **操作指南**：请依据需求分析阶段的标签勾选。勾选后，必须在"第 3 节"提供对应的测试用例或方案。

**[已完成]**

* [X] **功能自检测试**

> * **测试重点：** API 契约验证、业务逻辑分支覆盖、边界值测试。
>* **目的：** 确保功能实现符合设计预期。
>* **触发条件：** 强制执行,**可委托开发测试完成，测试完成验收**。

* [ ] **体验测试**

> * **测试重点：** 站在用户角度进行体验使用，验证产品是否符合用户习惯。
>* **目的：** 满足用户需求，超出用户期望；判定产品是否能让用户快速的接受和使用。
>* **触发条件：** 需求标签含 `need_experience`

* [X] **集成测试**

> * **测试重点：** 跨服务调用链路验证、上下游数据最终一致性、数据库/中间件版本兼容性。
>* **目的：** 消除组件间级联影响风险。
>* **触发条件：** 需求标签含 `need_itest`

* [ ] **安全与隐私测试**：

> * **测试重点：** 鉴权绕过测试、SQL/命令注入、敏感数据（PII）日志脱敏校验、SBOM 依赖漏洞扫描。
>* **目的：** 验证"纵深防御"机制是否生效，确保无隐私泄露。
>* **触发条件：** 需求标签含 `need_security`

* [ ] **可靠性与韧性测试**

> * **测试重点：** 故障注入（Chaos）。模拟网络丢包/延迟、进程意外溢出、磁盘 IO 满载后等异常情况下的系统自愈行为。
>* **目的：** 验证架构设计中的"面向失败设计"等能力。
>* **触发条件：** 涉及核心Core服务变更,且架构设计含可靠性与韧性设计。

* [ ] **可服务性与可观测性测试**

> * **测试重点：** 告警有效性验证、指标准确性抽检、排障手册实操演练、优雅停机验证。
>* **目的：** 确保系统"可感知、可定位、可维护"。
>* **触发条件：**  涉及核心Core服务变更,且架构设计含可服务性与可观测性设计。

* [ ] **性能与伸缩性测试**

> * **测试重点：** 基准测试、负载测试。验证延迟、吞吐量上限及资源配额（CPU/Mem）稳定性。
>* **目的：** 确保不产生性能退化，满足 SLO 要求。
>* **触发条件：** 涉及核心Core服务变更,且架构设计含性能与伸缩性设计。

---

## 3. 专项验证设计和执行详情

> 测试自检
>* [ ] **Task 闭环**: 架构设计说明书中定义的 **TASK** 是否均有对应的测试结果？
>* [ ] **证据留存**: 关键测试（如性能、安全扫描）是否附带了截图或报告链接？

### 3.1 功能测试专项

> 参考测试设计方向
> * API 语义验证：验证 HTTP 状态码（2xx, 4xx, 5xx）的使用是否符合 RESTful 规范。
> * 边界与非法输入：验证大数据量、空字段、特殊字符及非法 JSON 格式的拦截能力。
> * 业务状态机闭环：验证资源从"创建中"到"运行中"再到"已释放"的全生命周期逻辑。

**[已完成]**

**1. 组织人员信息MCP Server接口验证**

* **对应task链接**: https://github.com/opensourceways/backlog/issues/108 (TASK1 #108-1)
* **测试场景**:
  - 调用 `list_org_types()` 返回所有组织类型
  - 调用 `list_organizations(org_type)` 返回指定类型的组织列表
  - 调用 `get_org_detail(org_id)` 返回组织详细信息
  - 调用 `get_org_members(org_id)` 返回组织成员列表
  - 调用 `get_meeting_rules(org_id)` 返回会议规则
* **预期结果**:
  - 所有接口返回数据格式符合规范
  - 组织类型列表包含预期的类型（board, expert_committee, user_committee, tsc, tc, sig等）
  - 组织详情包含成员列表、会议频率要求、meeting_url、Etherpad链接等完整信息
  - 成员列表包含role字段（chair/maintainer/member等）
  - 会议规则包含会议频率、议程要求、纪要要求等信息

**2. 会议记录MCP Server接口验证**

* **对应task链接**: https://github.com/opensourceways/backlog/issues/108 (TASK2 #108-2)
* **测试场景**:
  - 调用 `list_meeting_schedules(org_id, period)` 查询会议预约记录
  - 调用 `get_meeting_attendance(meeting_id)` 查询会议出席情况
  - 调用 `get_last_meeting(org_id)` 查询最近一次会议
* **预期结果**:
  - 会议列表包含时间、主题、参会人信息
  - 出席信息包含实际参会人数和出席名单
  - 最近会议信息包含会议时间、主题、出席人数

**3. 会议纪要MCP Server接口验证**

* **对应task链接**: https://github.com/opensourceways/backlog/issues/108 (TASK2 #108-2)
* **测试场景**:
  - 调用 `get_pad_content(pad_name)` 获取pad内容
  - 调用 `get_pad_last_updated(pad_name)` 获取最后更新时间
  - 调用 `list_org_pads(org_id)` 列出组织关联的pad
* **预期结果**:
  - pad内容返回完整的Markdown/纯文本
  - 更新时间返回正确的时间戳
  - pad列表包含pad名称和对应会议日期

**4. 邮件列表MCP Server接口验证**

* **对应task链接**: https://github.com/opensourceways/backlog/issues/108 (TASK3 #108-3)
* **测试场景**:
  - 调用 `search_emails(list_id, keywords, date_from, date_to)` 搜索邮件
  - 调用 `get_email_detail(email_id)` 获取邮件详情
  - 调用 `list_mailing_lists(org_id)` 列出邮件列表
* **预期结果**:
  - 邮件搜索结果包含发件人、主题、时间信息
  - 邮件详情返回完整的邮件正文
  - 邮件列表返回组织关联的邮件列表地址

**5. MCP协议兼容性验证**

* **对应task链接**: https://github.com/opensourceways/backlog/issues/108 (TASK1-3)
* **测试场景**:
  - 使用MCP客户端连接4个Server
  - 调用Tool接口验证参数传递和返回值
* **预期结果**:
  - 所有Server能够正常接受MCP连接
  - Tool调用参数正确传递，返回值符合规范

### 3.2 体验测试专项

> 参考测试设计方向
> * 无效引导：是否有空数据界面设计，引导用户去执行操作；是否滥用用户引导；是否有不可点击的效果。
> * 页面易用性：菜单层次是否太深；网站页面是否过长，内容过多，引起浏览者视觉疲劳；一次是否载入太多的数据；交互流程分支是否太多。
> * 设计易用性：标签页是否跟内容没有从属关系，当切换标签的时候，内容跟着切换；操作应该有主次从属关系；导航是否友好，不知如何返回上一页，不知当前页面在哪个栏目下；过于复杂的验证码，不清晰的验证码。
> * 页面性能：页面加载，下载时间是否过长，是否在合理的加载范围内；页面是否有过多新窗口，大量占用计算机资源。
> * 页面信息：是否有死链接或者链接错误；是否有页面安全信息；过于复杂的验证码，不清晰的验证码。

**[不涉及，原因：本需求为后端MCP Server开发，不涉及用户界面交互]**

### 3.3 集成测试专项

> **第二节未勾选应直接删除**

> 参考测试设计方向
> * 协议兼容性：验证不同版本的组件间（如旧版网关与新版后端）通过 gRPC/HTTP 通信的兼容性。
> * 分布式事务/最终一致性：验证跨数据库操作或跨服务调用时，在网络波动下的数据对齐情况。
> * 全链路追踪覆盖：确保 TraceID 在所有微服务节点间透传，无断链现象。

**[已完成]**

**1. 端到端数据查询流程验证**

* **对应task链接**: https://github.com/opensourceways/backlog/issues/108 (TASK1-3)
* **测试场景**:
  - AI Agent通过MCP协议查询组织人员信息
  - AI Agent通过MCP协议查询会议记录
  - AI Agent通过MCP协议查询会议纪要
  - AI Agent通过MCP协议查询邮件列表
* **预期结果**:
  - AI Agent能够成功连接4个MCP Server
  - 查询请求能够正确路由到对应的数据源
  - 数据从数据源正确流向AI Agent
  - 返回数据格式符合MCP协议规范

**2. 多数据源并发查询验证**

* **对应task链接**: https://github.com/opensourceways/backlog/issues/108 (TASK1-3)
* **测试场景**:
  - AI Agent同时查询多个组织的会议记录
  - AI Agent同时查询多个pad的内容
  - AI Agent同时搜索多个邮件列表
* **预期结果**:
  - 并发查询能够正确处理，无数据混淆
  - 每个查询返回正确的数据源数据
  - 查询响应时间在可接受范围内

**3. 数据源连接异常处理验证**

* **对应task链接**: https://github.com/opensourceways/backlog/issues/108 (TASK1-3)
* **测试场景**:
  - 模拟组织数据库连接失败
  - 模拟会议预约系统连接失败
  - 模拟Etherpad连接失败
  - 模拟邮件列表系统连接失败
* **预期结果**:
  - 连接失败时返回明确的错误信息
  - 错误信息包含失败原因和数据源标识
  - 其他数据源不受影响，能够正常查询


### 3.4 安全与隐私测试专项

> **第二节未勾选应直接删除**

> 参考测试设计方向
> * 最小特权原则 (PoLP)：验证服务账户（Service Account）是否无法越权操作未授权的资源。
> * 软件供应链审计：通过 SBOM 检查，验证容器镜像中是否存在已知的高危 CVE 漏洞。
> * 敏感信息外泄防护：检查日志、监控指标及 API 报错中是否夹带了明文密钥或隐私数据。

**[不涉及，原因：需求分析未勾选need_security标签]**

### 3.5 可靠性与韧性专项

> **第二节未勾选应直接删除**

> 参考测试设计方向
> * 优雅降级与熔断：验证当外部依赖响应极慢时，系统是否能快速切换到"断路"状态以保护自身。
> * 自愈能力 (Chaos Engineering)：验证 Pod 被随机 Kill 或节点网络分区后，流量是否能自动漂移并恢复。
> * 幂等性验证：验证同一请求重试多次后，系统状态和数据是否保持唯一，无重复记录。

**[不涉及，原因：架构设计中未包含可靠性与韧性设计]**

### 3.6 可服务性与可观测性专项

> **第二节未勾选应直接删除**

> 参考测试设计方向
> * 告警闭环验证：验证从指标触发阈值到告警推送（通知到人）的全流程时效性。
> * 资源优雅退出：验证服务在重启过程中，是否能确保存量请求处理完后再断开连接。
> * 排障手册 (Runbook) 校验：验证运维人员能否仅凭监控图表和文档快速定位根因。

**[不涉及，原因：架构设计中未包含可服务性与可观测性设计]**

### 3.7 性能与可伸缩性专项

> **第二节未勾选应直接删除**

> 参考测试设计方向
> * 吞吐量与时延基准：验证在额定并发下，P99 延迟是否满足服务水平协议（SLA）。
> * 自动扩缩容效率：验证 HPA（水平扩容）从触发阈值到新副本就绪的响应时间。
> * 长期稳定性 (Soak Test)：验证在持续负载下，是否存在内存缓慢泄漏或连接数不释放的问题。

**[不涉及，原因：架构设计中未包含性能与伸缩性设计]**

---
