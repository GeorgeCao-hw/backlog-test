# #133 社区热点管理MCP集成 测试策略设计说明书

## 1. 基本信息

* **需求链接**: https://github.com/opensourceways/backlog/issues/133
* **需求名称**: 社区热点管理MCP集成_支持GEO优化获取TopN热点问题
* **核心目标**:
  验证功能正确性，以及架构设计中定义的安全与隐私、可靠性与韧性、可服务性与可观测性和性能与伸缩性等非功能专项任务的闭环验收。
* **开发责任人**: Kaede10
* **测试责任人**: guoxiaozhen0304

---

## 2. 测试维度确认

> **操作指南**：请依据需求分析阶段的标签勾选。勾选后，必须在"第 3 节"提供对应的测试用例或方案。

* [X) **功能自检测试**

> * **测试重点：** API 契约验证、业务逻辑分支覆盖、边界值测试。
>* **目的：** 确保功能实现符合设计预期。
>* **触发条件：** 强制执行,**可委托开发测试完成，测试完成验收**。

* [ ] **体验测试**

> * **测试重点：** 站在用户角度进行体验使用，验证产品是否符合用户习惯。
>* **目的：** 满足用户需求，超出用户期望；判定产品是否能让用户快速的接受和使用。
>* **触发条件：** 需求标签含 `need_experience`

* [X) **集成测试**

> * **测试重点：** 跨服务调用链路验证、上下游数据最终一致性、数据库/中间件版本兼容性。
>* **目的：** 消除组件间级联影响风险。
>* **触发条件：** 需求标签含 `need_itest`

* [X) **安全与隐私测试**：

> * **测试重点：** 鉴权绕过测试、SQL/命令注入、敏感数据（PII）日志脱敏校验、SBOM 依赖漏洞扫描。
>* **目的：** 验证"纵深防御"机制是否生效，确保无隐私泄露。
>* **触发条件：** 需求标签含 `need_security`

* [ ] **可靠性与韧性测试**

> * **测试重点：** 故障注入（Chaos）。模拟网络丢包/延迟、进程意外溢出、磁盘 IO 满载后等异常情况下的系统自愈行为。
>* **目的：** 验证架构设计中的"面向失败设计"等能力。
>* **触发条件：** 涉及核心Core服务变更,且架构设计含可靠性与韧性设计。

* [ ] **可服务性与可观测性测试**

> * **测试重点：** 告警有效性验证、指标准确性抽检、排障手册实操演练、优雅停机验证。
>* **目的：** 确保系统"可感知、可定位、可维护"。
>* **触发条件：**  涉及核心Core服务变更,且架构设计含可服务性与可观测性设计。

* [ ] **性能与伸缩性测试**

> * **测试重点：** 基准测试、负载测试。验证延迟、吞吐量上限及资源配额（CPU/Mem）稳定性。
>* **目的：** 确保不产生性能退化，满足 SLO 要求。
>* **触发条件：** 涉及核心Core服务变更,且架构设计含性能与伸缩性设计。

---

## 3. 专项验证设计和执行详情

> 测试自检
>* [ ] **Task 闭环**: 架构设计说明书中定义的 **TASK** 是否均有对应的测试结果？
>* [ ] **证据留存**: 关键测试（如性能、安全扫描）是否附带了截图或报告链接？

### 3.1 功能测试专项

> 参考测试设计方向
> * API 语义验证：验证 HTTP 状态码（2xx, 4xx, 5xx）的使用是否符合 RESTful 规范。
> * 边界与非法输入：验证大数据量、空字段、特殊字符及非法 JSON 格式的拦截能力。
> * 业务状态机闭环：验证资源从"创建中"到"运行中"再到"已释放"的全生命周期逻辑。

**1. API权限验证中间件双重验证逻辑**

* **对应task**: TASK1 - 修改API权限验证中间件，支持Token和登录双重验证（OR逻辑）
* **测试场景**:
  - 场景1：携带有效Token的请求，验证通过
  - 场景2：携带无效Token但登录状态有效的请求，验证通过
  - 场景3：携带无效Token且无登录状态的请求，验证失败返回401
  - 场景4：不携带Token但登录状态有效的请求，验证通过
* **预期结果**:
  - 场景1：返回200 OK，API正常响应
  - 场景2：返回200 OK，API正常响应
  - 场景3：返回401 Unauthorized，拒绝访问
  - 场景4：返回200 OK，API正常响应

**2. MCP Token管理和API调用**

* **对应task**: TASK2 - 实现MCP Token管理逻辑，配置MCP服务调用API时携带Token
* **测试场景**:
  - 场景1：MCP服务启动时从安全存储成功读取Token
  - 场景2：MCP调用API时请求Header正确携带Token（Authorization: Bearer {token}）
  - 场景3：MCP通过API文档自动注册TopN问题查询Tool
  - 场景4：MCP成功调用API并获取TopN问题数据
* **预期结果**:
  - 场景1：Token成功加载到内存，无错误日志
  - 场景2：通过抓包或日志验证Header包含正确的Token
  - 场景3：MCP Tool列表中包含TopN问题查询工具
  - 场景4：返回TopN问题数据，数据格式符合预期

### 3.2 体验测试专项

> 参考测试设计方向
> * 无效引导：是否有空数据界面设计，引导用户去执行操作；是否滥用用户引导；是否有不可点击的效果。
> * 页面易用性：菜单层次是否太深；网站页面是否过长，内容过多，引起浏览者视觉疲劳；一次是否载入太多的数据；交互流程分支是否太多。
> * 设计易用性：标签页是否跟内容没有从属关系，当切换标签的时候，内容跟着切换；操作应该有主次从属关系；导航是否友好，不知如何返回上一页，不知当前页面在哪个栏目下；过于复杂的验证码，不清晰的验证码。
> * 页面性能：页面加载，下载时间是否过长，是否在合理的加载范围内；页面是否有过多新窗口，大量占用计算机资源。
> * 页面信息：是否有死链接或者链接错误；是否有页面安全信息；过于复杂的验证码，不清晰的验证码。

不涉及，原因：本需求为后端API和MCP集成，无用户界面交互。

### 3.3 集成测试专项

> 参考测试设计方向
> * 协议兼容性：验证不同版本的组件间（如旧版网关与新版后端）通过 gRPC/HTTP 通信的兼容性。
> * 分布式事务/最终一致性：验证跨数据库操作或跨服务调用时，在网络波动下的数据对齐情况。
> * 全链路追踪覆盖：确保 TraceID 在所有微服务节点间透传，无断链现象。

**1. MCP到API的端到端流程验证**

* **对应task**: TASK1, TASK2
* **测试场景**:
  - 场景1：MCP Client请求TopN问题 → MCP Server读取Token → 携带Token调用API → 返回数据
  - 场景2：验证完整数据流，从MCP Client到API再到数据库的完整链路
  - 场景3：验证API响应数据格式正确，MCP Client能正确解析
* **预期结果**:
  - 场景1：端到端流程成功，MCP Client收到TopN问题数据
  - 场景2：每个环节的日志和监控数据正常，无错误
  - 场景3：数据格式符合API文档定义，字段完整

**2. Token和登录验证的双重验证逻辑验证**

* **对应task**: TASK1
* **测试场景**:
  - 场景1：Token验证路径：MCP携带Token → API验证Token → 放行
  - 场景2：登录验证路径：用户登录 → API验证Session → 放行
  - 场景3：Token和登录均失败：返回401未授权
  - 场景4：Token和登录均有效：优先使用Token验证
* **预期结果**:
  - 场景1：Token验证成功，API正常响应
  - 场景2：登录验证成功，API正常响应
  - 场景3：返回401 Unauthorized，拒绝访问
  - 场景4：Token验证优先，登录验证作为备选

### 3.4 安全与隐私测试专项

> 参考测试设计方向
> * 最小特权原则 (PoLP)：验证服务账户（Service Account）是否无法越权操作未授权的资源。
> * 软件供应链审计：通过 SBOM 检查，验证了容器镜像中是否存在已知的高危 CVE 漏洞。
> * 敏感信息外泄防护：检查日志、监控指标及 API 报错中是否夹带了明文密钥或隐私数据。

**1. Token安全存储验证**

* **对应task**: SEC-TASK1 - 实现Token安全存储逻辑，从Kubernetes Secret/Vault读取Token
* **测试场景**:
  - 场景1：检查代码中无硬编码Token
  - 场景2：验证Token从Kubernetes Secret/Vault成功读取
  - 场景3：验证Token存储权限设置正确（仅授权用户可读）
  - 场景4：验证Token在运行时驻留在内存中，不写入临时文件
* **预期结果**:
  - 场景1：代码搜索无明文Token
  - 场景2：Token成功读取，无权限错误
  - 场景3：Secret/Vault权限为600或640
  - 场景4：临时文件目录无Token相关文件

**2. API权限验证中间件安全性验证**

* **对应task**: SEC-TASK2 - 修改API权限验证中间件，支持Token和登录双重验证（OR逻辑）
* **测试场景**:
  - 场景1：模拟伪造Token请求，验证被拒绝
  - 场景2：模拟无Token且无登录状态的请求，验证返回401
  - 场景3：验证Token验证失败不泄露具体错误信息
  - 场景4：验证登录验证失败不泄露具体错误信息
* **预期结果**:
  - 场景1：返回401 Unauthorized，拒绝访问
  - 场景2：返回401 Unauthorized，拒绝访问
  - 场景3：错误消息不包含Token信息，仅返回"Unauthorized"
  - 场景4：错误消息不包含登录信息，仅返回"Unauthorized"

**3. 日志脱敏验证**

* **对应task**: SEC-TASK3 - 实现日志脱敏，Token和敏感字段不记录或仅记录部分掩码
* **测试场景**:
  - 场景1：触发API调用，检查日志中无完整Token
  - 场景2：验证Token在日志中仅记录部分掩码（如：`sk-****abcd`）
  - 场景3：验证敏感字段（如用户信息）在日志中脱敏
  - 场景4：验证审计日志包含时间戳、调用方、API端点、结果状态
* **预期结果**:
  - 场景1：日志搜索无完整Token字符串
  - 场景2：日志中Token格式为`sk-****abcd`或类似掩码
  - 场景3：敏感字段被脱敏或替换为`***`
  - 场景4：审计日志包含4W信息（Who, When, Where, What）

**4. MCP调用API携带Token验证**

* **对应task**: SEC-TASK4 - 配置MCP服务调用API时携带Token，确保HTTPS传输
* **测试场景**:
  - 场景1：抓包验证MCP调用API时使用HTTPS协议
  - 场景2：验证请求Header包含Authorization: Bearer {token}
  - 场景3：验证Token格式正确，符合API要求
  - 场景4：验证API响应数据不包含敏感信息
* **预期结果**:
  - 场景1：抓包显示协议为HTTPS，TLS版本≥1.2
  - 场景2：Header中包含Authorization字段，格式正确
  - 场景3：Token格式符合API文档要求
  - 场景4：响应数据仅包含TopN问题元数据，无敏感信息

**5. API调用审计日志验证**

* **对应task**: SEC-TASK5 - 添加API调用审计日志，记录时间戳、调用方、API端点、结果状态
* **测试场景**:
  - 场景1：触发API调用，验证审计日志生成
  - 场景2：验证审计日志包含时间戳、调用方、API端点、结果状态
  - 场景3：验证审计日志不包含完整Token
  - 场景4：验证验证失败事件记录安全日志
* **预期结果**:
  - 场景1：审计日志成功生成，无错误
  - 场景2：审计日志包含所有4W信息
  - 场景3：审计日志中Token被脱敏或掩码
  - 场景4：安全日志记录验证失败事件，便于审计

### 3.5 可靠性与韧性专项

> 第二节未勾选应直接删除

> 参考测试设计方向
> * 优雅降级与熔断：验证当外部依赖响应极慢时，系统是否能快速切换到"断路"状态以保护自身。
> * 自愈能力 (Chaos Engineering)：验证 Pod 被随机 Kill 或节点网络分区后，流量是否能自动漂移并恢复。
> * 幂等性验证：验证同一请求重试多次后，系统状态和数据是否保持唯一，无重复记录。

不涉及，原因：本需求为API权限验证和MCP集成，不涉及高可用或容灾设计。

### 3.6 可服务性与可观测性专项

> 第二节未勾选应直接删除

> 参考测试设计方向
> * 告警闭环验证：验证从指标触发阈值到告警推送（通知到人）的全流程时效性。
> * 资源优雅退出：验证服务在重启过程中，是否能确保存量请求处理完后再断开连接。
> * 排障手册 (Runbook) 校验：验证运维人员能否仅凭监控图表和文档快速定位根因。

不涉及，原因：本需求为后端API和MCP集成，不涉及长驻服务的可观测性设计。

### 3.7 性能与可伸缩性专项

> 第二节未勾选应直接删除

> 参考测试设计方向
> * 吞吐量与时延基准：验证在额定并发下，P99 延迟是否满足服务水平协议（SLA）。
> * 自动扩缩容效率：验证 HPA（水平扩容）从触发阈值到新副本就绪的响应时间。
> * 长期稳定性 (Soak Test)：验证在持续负载下，是否存在内存缓慢泄漏或连接数不释放的问题。

不涉及，原因：本需求为API权限验证和MCP集成，不涉及高并发或性能优化。

---
