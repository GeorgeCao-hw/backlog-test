# #80 openEuler社区支持闭门会议 测试策略设计说明书

## 1. 基本信息

* **需求链接**: https://github.com/opensourceways/backlog/issues/80
* **需求名称**: openEuler社区支持闭门会议
* **核心目标**:
  验证闭门会议功能的正确性，包括创建/更新接口的参数校验逻辑、业务约束（平台限制、周期限制、邮件列表限制）、Kafka 消息抑制、公开查询接口过滤等核心行为是否符合需求设计预期，确保闭门会议信息不对外泄露。
* **开发责任人**: Tom_zc
* **测试责任人**: guoxiaozhen0304

---

## 2. 测试维度确认

> **操作指南**：依据需求分析阶段标签（`need_design`、`need_itest`、`need_ux`）勾选测试维度。

* [X] **功能自检测试**

> * **测试重点：** API 契约验证、业务逻辑分支覆盖、边界值测试。
> * **目的：** 确保功能实现符合设计预期。
> * **触发条件：** 强制执行，**可委托开发测试完成，测试完成验收**。

* [X] **体验测试**

> * **测试重点：** 站在用户角度进行体验使用，验证产品是否符合用户习惯。
> * **目的：** 满足用户需求，超出用户期望；判定产品是否能让用户快速接受和使用。
> * **触发条件：** 需求标签含 `need_ux`（新增 `is_private` 参数，用户创建会议时需选择是否为闭门会议，涉及前端交互变更）。

* [X] **集成测试**

> * **测试重点：** 跨服务调用链路验证、上下游数据最终一致性、数据库/中间件版本兼容性。
> * **目的：** 消除组件间级联影响风险。
> * **触发条件：** 需求标签含 `need_itest`（端到端链路：API 入参校验 → 业务逻辑处理 → 数据库存储/查询）。

* [ ] **安全与隐私测试**

> * **测试重点：** 鉴权绕过测试、SQL/命令注入、敏感数据（PII）日志脱敏校验、SBOM 依赖漏洞扫描。
> * **目的：** 验证"纵深防御"机制是否生效，确保无隐私泄露。
> * **触发条件：** 需求标签含 `need_security`（**本需求未触发**，闭门会议通过字段标记实现数据过滤，不涉及个人敏感数据存储或权限模型变更）。

* [ ] **可靠性与韧性测试**

> * **测试重点：** 故障注入（Chaos）。模拟网络丢包/延迟、进程意外溢出、磁盘 IO 满载后等异常情况下的系统自愈行为。
> * **目的：** 验证架构设计中的"面向失败设计"等能力。
> * **触发条件：** 涉及核心 Core 服务变更，且架构设计含可靠性与韧性设计（**本需求未触发**）。

* [ ] **可服务性与可观测性测试**

> * **测试重点：** 告警有效性验证、指标准确性抽检、排障手册实操演练、优雅停机验证。
> * **目的：** 确保系统"可感知、可定位、可维护"。
> * **触发条件：** 涉及核心 Core 服务变更，且架构设计含可服务性与可观测性设计（**本需求未触发**）。

* [ ] **性能与伸缩性测试**

> * **测试重点：** 基准测试、负载测试。验证延迟、吞吐量上限及资源配额（CPU/Mem）稳定性。
> * **目的：** 确保不产生性能退化，满足 SLO 要求。
> * **触发条件：** 涉及核心 Core 服务变更，且架构设计含性能与伸缩性设计（**本需求未触发**）。

---

## 3. 专项验证设计和执行详情

> 测试自检
> * [ ] **Task 闭环**: 需求分析说明书中定义的 TASK1～TASK4 是否均有对应的测试结果？
> * [ ] **证据留存**: 关键测试（如单元测试覆盖率报告）是否附带了截图或报告链接？

---

### 3.1 功能测试专项

> 参考测试设计方向
> * API 语义验证：验证 HTTP 状态码（2xx, 4xx, 5xx）的使用是否符合 RESTful 规范。
> * 边界与非法输入：验证非法参数组合、空字段、类型错误的拦截能力。
> * 业务状态机闭环：验证闭门会议创建 → 更新 → 查询过滤的全生命周期逻辑。

---

**1. 创建闭门会议（正常路径）**

验证携带 `is_private=true` 且满足所有约束条件时，闭门会议能成功创建，数据库中 `is_private` 字段值为 `True`。

* **对应 Task**: TASK1（模型层字段新增）、TASK2（Serializer 校验）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 调用创建会议接口，携带参数 `is_private=true`、`platform=WeLink`、`is_cycle=false`、`email_list` 不含社区邮件列表地址。
  2. 查询数据库，验证新增记录的 `is_private` 字段。
* **预期结果**: 接口返回 `2XX`，数据库记录中 `is_private=True`。

---

**2. 平台限制验证（Zoom 平台创建闭门会议）**

验证在 Zoom 平台创建闭门会议时，接口返回正确错误码。

* **对应 Task**: TASK2（Serializer 平台校验逻辑）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 调用创建会议接口，携带参数 `is_private=true`、`platform=Zoom`。
* **预期结果**: 接口返回 `4XX`，错误码为 `STATUS_MEETING_PRIVATE_SUPPORT_TYPE`，错误信息为"闭门会议只支持WeLink会议"。

---

**3. 平台限制验证（Tencent 平台创建闭门会议）**

验证在腾讯会议平台创建闭门会议时，接口返回正确错误码。

* **对应 Task**: TASK2（Serializer 平台校验逻辑）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 调用创建会议接口，携带参数 `is_private=true`、`platform=Tencent`。
* **预期结果**: 接口返回 `4XX`，错误码为 `STATUS_MEETING_PRIVATE_SUPPORT_TYPE`，错误信息为"闭门会议只支持WeLink会议"。

---

**4. 周期性限制验证**

验证创建周期性闭门会议时，接口返回正确错误码。

* **对应 Task**: TASK2（Serializer 周期校验逻辑）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 调用创建会议接口，携带参数 `is_private=true`、`platform=WeLink`、`is_cycle=true`。
* **预期结果**: 接口返回 `4XX`，错误码为 `STATUS_MEETING_PRIVATE_SUPPORT_CYCLE`，错误信息为"闭门会议只支持非周期性会议"。

---

**5. 邮件列表限制验证（含社区邮件列表地址）**

验证闭门会议携带社区邮件列表地址时，接口返回正确错误码。

* **对应 Task**: TASK2（Serializer 邮件列表校验）、TASK3（`check_email_in_list` 函数）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 调用创建会议接口，携带参数 `is_private=true`、`platform=WeLink`、`is_cycle=false`、`email_list` 包含 `dev@openeuler.org`。
* **预期结果**: 接口返回 `4XX`，错误码为 `STATUS_MEETING_PRIVATE_SUPPORT_EMAIL_LIST`，错误信息为"闭门会议不支持通过邮件列表通知会议"。

---

**6. 邮件列表边界验证（非社区邮件列表地址）**

验证闭门会议携带非社区邮件列表的个人邮件地址时，不触发限制。

* **对应 Task**: TASK2、TASK3（`check_email_in_list` 函数边界逻辑）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 调用创建会议接口，携带参数 `is_private=true`、`platform=WeLink`、`is_cycle=false`、`email_list` 仅包含个人邮件地址（如 `user@example.com`）。
* **预期结果**: 接口返回 `2XX`，会议创建成功。

---

**7. 公开会议列表查询过滤验证（get_meeting_date 接口）**

验证 `get_meeting_date` 查询接口不返回闭门会议记录。

* **对应 Task**: TASK3（业务层查询过滤逻辑）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 预置数据：创建一条 `is_private=true` 的闭门会议和一条 `is_private=false` 的公开会议。
  2. 调用 `get_meeting_date` 接口。
* **预期结果**: 响应结果中仅包含公开会议记录，闭门会议不出现在返回列表中。

---

**8. 公开会议列表查询过滤验证（get_meeting_group_name 接口）**

验证 `get_meeting_group_name` 查询接口不返回闭门会议记录。

* **对应 Task**: TASK3（DAO 层 `get_meeting_group_name` 过滤条件）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 预置数据：创建一条 `is_private=true` 的闭门会议和一条公开会议（相同 group_name）。
  2. 调用 `get_meeting_group_name` 接口。
* **预期结果**: 响应结果中闭门会议不出现，仅返回 `is_private=false` 的会议。

---

**9. 更新现有会议为闭门会议的校验一致性**

验证更新接口对闭门会议的约束校验逻辑与创建接口一致。

* **对应 Task**: TASK3（业务层更新时平台校验）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 预置一条 WeLink 公开会议。
  2. 调用更新接口，将 `is_private` 修改为 `true`，同时 `platform` 修改为 `Zoom`。
* **预期结果**: 接口返回 `4XX`，错误码为 `STATUS_MEETING_PRIVATE_SUPPORT_TYPE`，行为与创建接口一致。

---

**10. 单元测试覆盖率验证**

验证 TASK4 编写的单元测试 statement 覆盖率满足验收标准。

* **对应 Task**: TASK4（单元测试文件 `test_private_meeting.py`）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 执行单元测试，生成 statement 覆盖率报告。
* **预期结果**: 覆盖率 ≥ 80%，覆盖闭门会议创建成功、平台限制、周期限制、邮件列表限制、查询过滤等主要分支。

---

**11. `is_private` 参数类型校验**

验证 `is_private` 参数传入非布尔类型时，Serializer 层正确拦截。

* **对应 Task**: TASK2（`validate_is_private` 方法）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 调用创建会议接口，`is_private` 分别传入字符串 `"yes"`、数字 `1`、空值 `null`。
* **预期结果**: 接口返回 `4XX`，提示参数类型错误。

---

### 3.2 体验测试专项

> 参考测试设计方向
> * 交互逻辑：新增字段是否有明确的 UI 提示，引导用户理解"闭门会议"的含义及约束。
> * 错误提示语：3 个错误码的提示文案是否清晰，用户能否根据提示快速定位操作问题。
> * 前端联动逻辑：选择非 WeLink 平台时，`is_private` 选项是否有禁用/提示处理。

---

**1. 闭门会议选项的前端交互引导**

验证用户在创建会议时，能清晰发现并理解"闭门会议"选项的功能含义。

* **对应 Task**: TASK1（`is_private` 字段新增对前端的影响）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **预期结果**: 页面上"闭门会议"选项有明确的标签说明或 Tooltip 提示，用户无需查阅文档即可理解其作用。

---

**2. 平台限制错误提示语的易读性**

验证在非 WeLink 平台勾选闭门会议时，前端展示的错误提示语清晰易懂。

* **对应 Task**: TASK3（新增错误码 `STATUS_MEETING_PRIVATE_SUPPORT_TYPE`）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **预期结果**: 错误提示语"闭门会议只支持WeLink会议"准确展示，用户能快速定位并修改操作，无歧义。

---

**3. 周期性会议限制的前端联动处理**

验证选择"周期性会议"后，"闭门会议"选项是否有禁用或冲突提示。

* **对应 Task**: TASK2（周期限制校验）、前端交互逻辑
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **预期结果**: 勾选"周期性会议"后，"闭门会议"选项置灰不可选，或在提交时给出清晰的错误引导，避免用户反复尝试。

---

### 3.3 集成测试专项

> 参考测试设计方向
> * 全链路追踪：验证从 API 入参到数据库持久化的完整链路数据一致性。
> * 副作用抑制验证：验证闭门会议创建后，不触发 Kafka 消息广播和邮件列表通知。
> * 正常会议流程不受影响：验证新增闭门会议逻辑后，公开会议的创建、查询、Kafka 推送等功能仍正常工作。

---

**1. 创建闭门会议时 Kafka 消息抑制验证**

验证成功创建闭门会议后，不向 Kafka 发送会议创建消息。

* **对应 Task**: TASK3（业务层 Kafka 消息抑制逻辑）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 监控 Kafka 对应 Topic 消费情况。
  2. 调用创建会议接口，携带 `is_private=true`，成功创建闭门会议。
  3. 观察 Kafka Topic 是否产生新消息。
* **预期结果**: Kafka 对应 Topic 中无新增会议创建消息，闭门会议信息不被广播。

---

**2. 创建闭门会议时邮件列表通知抑制验证**

验证成功创建闭门会议后，不发送邮件列表通知。

* **对应 Task**: TASK3（业务层邮件通知抑制逻辑）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 调用创建会议接口，携带 `is_private=true`，成功创建闭门会议。
  2. 检查邮件发送服务日志或邮件列表收件箱。
* **预期结果**: 邮件发送服务未触发，邮件列表未收到该会议通知邮件。

---

**3. 公开会议创建流程不受影响**

验证新增闭门会议逻辑后，`is_private=false` 的公开会议创建流程及 Kafka 推送行为不受影响。

* **对应 Task**: TASK1、TASK2、TASK3（回归验证）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 调用创建会议接口，不携带 `is_private` 参数（默认 False）或显式传 `is_private=false`。
  2. 检查 Kafka 消息推送和邮件通知。
* **预期结果**: 公开会议正常创建，Kafka 消息正常推送，邮件列表通知正常发送，行为与变更前一致。

---

**4. 全链路数据一致性验证**

验证从 API 请求到数据库持久化的全链路数据正确传递。

* **对应 Task**: TASK1（模型层字段）、TASK2（Serializer 校验）、TASK3（业务层）
* **对应 Task 链接**: https://github.com/opensourceways/backlog/issues/80
* **测试步骤**:
  1. 调用创建会议接口，携带 `is_private=true` 及完整合法参数。
  2. 通过数据库查询接口或直接查询 DB 验证记录。
  3. 调用公开会议列表接口，验证该记录不出现。
* **预期结果**: 数据库中 `is_private=True` 记录正确写入；公开列表查询返回结果中该会议不可见，三个环节数据一致。

---
